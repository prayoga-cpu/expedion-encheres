import '/flutter_flow/flutter_flow_util.dart';
import 'page_contact_devis_widget.dart' show PageContactDevisWidget;
import 'package:flutter/material.dart';

/// Nothing left to hold.
///
/// This carried a form key and six controller/focus-node pairs for the contact
/// form the page used to render. The page is a redirect now; the model stays
/// only because `createModel` is what every FlutterFlow page is wired to.
class PageContactDevisModel extends FlutterFlowModel<PageContactDevisWidget> {
  @override
  void initState(BuildContext context) {}

  @override
  void dispose() {}
}
